/**
 * next.config.ts
 *
 * Next.js configuration for FitVibe 3D fitness app.
 * Standalone config - no database required.
 *
 * Related:
 * - app/layout.tsx
 */

import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  reactStrictMode: true,
}

export default nextConfig
